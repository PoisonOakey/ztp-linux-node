{{ ansible_managed | comment }}
# Rendered by the monitoring role. The password is generated once on the control
# node and reused on every subsequent run, so this file is stable and the stack
# is not restarted needlessly. Never commit a rendered copy of this file.
GF_SECURITY_ADMIN_PASSWORD={{ monitoring_grafana_admin_password }}
