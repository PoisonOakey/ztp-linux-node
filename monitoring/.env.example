# Copy this file to .env and set a real value before deploying manually.
# 06-setup-monitoring.ps1 does this automatically (with a generated password)
# if monitoring/.env doesn't already exist -- this file is just the template.
GF_SECURITY_ADMIN_PASSWORD=changeme
